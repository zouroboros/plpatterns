# Read file to string (nodejs)

![readFile.js](readFile.js "readFile.js")

You can use the `readFile` function to read a file into a string. The above
code prints the content of the file `fileToRead` to the console.