const lemonTree = {
    name: "Citrus lemon",
    fruit: "Lemon",
    origin: "Asia",
    taste: "sour",
    color: "yellow"
};

const { name, fruit, color } = lemonTree;

console.log(name, fruit, color);