const fruits = ["Apple", "Banana", "Cocos", "Dates", "Eggplant"];

// for of loop
for (const fruit of fruits) {
    console.log(fruit);
}

// loop using forEach
fruits.forEach(fruit => {
    console.log(fruit);
});
